import blockContent from "./blockContent";
import category from "./category";
import post from "./post";
import page from "./page";
import site from "./site";

export const schema = {
  types: [post, category, blockContent, page, site],
};
